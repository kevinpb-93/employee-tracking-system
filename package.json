{
  "name": "employee-tracking-system",
  "version": "1.0.0",
  "description": "Sistema de seguimiento de empleados",
  "main": "server.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.10.1"
  }
}